-- Vendored from LazyVim core (lazyvim/plugins/coding.lua). ts-comments is enabled in
-- disable.lua (non-win32); carries its base spec now that the LazyVim core import is gone.
-- Better `gc` comment handling using treesitter (context-aware commentstring).
return {
  "folke/ts-comments.nvim",
  event = "VeryLazy",
  opts = {},
}
